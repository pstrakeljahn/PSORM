<?php

use PS\Core\Rdw\RdwBasic;

class {{ className }}Basic extends RdwBasic {
    protected $ID;
    {%- for field in fields %}
    protected ${{ field }};
    {%- endfor %}

    public function getID() {
        return $this->ID;
    }

    {%- for field in fields %}

    public function get{{ field|capitalize }}() {
        return $this->{{ field }};
    }

    public function set{{ field|capitalize }}(${{ field }}) {
        $this->{{ field }} = ${{ field }};
    }
    {%- endfor %}
}
